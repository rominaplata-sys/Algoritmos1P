# Este programa da el valor a la pendiente (primer pronblema)
x1 = float(input("Introduce x1: "))
y1 = float(input("Introduce y1: "))
x2 = float(input("Introduce x2: "))
y2 = float(input("Introduce y2: "))

if x2 != x1:
    pendiente = (y2 - y1) / (x2 - x1)
    print("La pendiente entre los dos puntos es:", pendiente)
else:
    print("La pendiente es indefinida (división por cero).")
#Calcularla usando la formula m = (y2 - y1) / (x2 - x1)
m = (y2 - y1) / (x2 - x1)
#imprimmir el resultado
print("La pendiente entre los dos puntos es:", m)

#DIATANCIA ENTRE DOS PUNTOS (segundo problema)
distancia = ((x2 - x1)**2 + (y2 - y1)**2)
print("La distancia entre los dos puntos es:", distancia)
# Calcular la distancia usando la fórmula d = (x2 - x1)**2 + (y2 - y1)**2
d = (x2 - x1)**2 + (y2 - y1)**2
print("La distancia al cuadrado entre los dos puntos es:", d)
#Imprimir el valor de la distancia.
print("El valor de la distancia es:", distancia)

#Suma de digitos (tercer problema)
num = int(input("Introduce un número entero positivo: "))
suma = 0
while num > 0:
    digito = num % 10
    suma += digito
    num //= 10
#imprimir el resultado
print("La suma de los dígitos es:", suma)